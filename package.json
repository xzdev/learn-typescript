{
  "name": "learn-typescript",
  "version": "0.0.1",
  "description": "The example code for learning TypeScript",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "exec": "ts-node src/index.ts",
    "check": "gts check",
    "clean": "gts clean",
    "build": "tsc -p .",
    "fix": "gts fix",
    "prepare": "yarn build",
    "pretest": "yarn build",
    "posttest": "yarn check"
  },
  "devDependencies": {
    "@types/node": "^10.0.3",
    "gts": "^1.1.2",
    "prettier-eslint": "^9.0.1",
    "prettier-stylelint": "^0.4.2",
    "prettier-tslint": "^0.4.2",
    "ts-node": "^8.6.2",
    "typescript": "~3.7.0"
  }
}
